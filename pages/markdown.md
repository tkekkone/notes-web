# Marked in browser

Rendered by **marked**.

This page renders all pages in the content container from markdown files. They are generated client side on marked.js library. nice.

It is intended to be a lightweight blog engine. User can just edit the markdown files through their familiar shell and vim. These are then generated into this blog page. All files in a directory are listed on the navigation bar and arranged with modified time on the files.
